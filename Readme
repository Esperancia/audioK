#e9w86036f78sd9

##Juste un fichier de notes##

Pour démarrer: 
    - docker-compose up -d --build

Une fois le projet démarré, les autres fois :
    - docker-compose up -d

Voici les noms des containes

    - audiok_www_1 pour php:apache

    - audiok_db_pma_1 pour phpmyadmin

    - audiok_db_1 pour mysql

Pour entrer dans un container surtout le php:
    - docker exec -it audiok_www_1 bash

Notes pfff à moi même vu que j'oublie trop vite: 
- La bd se trouve dans le dossier database avec d'autres fichiers de mysql qui sont ignorés par git.
- la BD que j'ai créée est audiok et est répertoriée et suivie par git. D'où pour vider il faut supprimer ses tables.